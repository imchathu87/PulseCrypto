---
scope: PulseCrypto gateway (apps/api) + Expo Go viewer (apps/mobile) + packages/contracts
purpose: build-substrate; deliverables docs/architecture/architecture.md + docs/architecture/adr/ADR-001..012 per user
altitude: feature
updated: 2026-09-19T12:09
---

- (event) Fast path chosen by user. Deliverable = docs/architecture/architecture.md + 12 ADRs (user-specified paths) instead of ARCHITECTURE-SPINE.md; ADRs play the AD role.
- (event) Missing inputs: docs/requirements/, docs/reference/engineering-guide.md, docs/reference/toolchain.md, tag v0-skeleton. User approved substitutes: CAP-1..16 as requirement IDs, spec stack.md + lockfile as toolchain, commit b21a370 as skeleton, ADR numbering defined here with ADR-011 = simulator (fixed by testing-strategy.md).
- (decision) Client interval range 10-1000ms (spec CAP-15 wins over testing-strategy 50-2000ms, user decision); testing-strategy.md updated in same change.
- (decision) [ADOPTED] ADR-001 combined Binance stream; binds adapter; prevents unattributable depth frames (depth20 has no symbol).
- (decision) [ADOPTED] ADR-002 partial depth20@100ms, no diff-depth reconciliation.
- (decision) [ADOPTED] ADR-003 latest-value map, per-pair rev stamped from process-wide monotonic sequence, per-client cursor = single integer; O(pairs) state; no per-client queue; single process in-memory.
- (decision) [ADOPTED] ADR-004 full per-pair snapshots; market.snapshot on connect then market.batch of pairs with rev > cursor; per-client timer at client interval; per-pair JSON cached per rev.
- (decision) ADR-005 backpressure: bufferedAmount > soft limit skips tick without advancing cursor; > hard limit or consecutive skips beyond budget closes socket 4008 slow consumer. Limits are env config, tuned in stress test.
- (decision) [ADOPTED] ADR-006 JSON envelope {v,type,t}; Zod schemas in packages/contracts; validation placement per engineering-standards s6.
- (decision) ADR-007 spread, buy/sell pressure computed on server in domain; 24h change passthrough of ticker P; timestamp = server receive clock of last mutation (ms epoch), not ticker E.
- (decision) ADR-008 two independent reconnect policies, same algorithm (full-jitter exponential), different parameters and liveness signals; upstream data-silence watchdog; downstream status-heartbeat watchdog + AppState.
- (decision) [ADOPTED] ADR-009 Zustand market store, one commit per inbound WS message; TanStack Query for /pairs/meta; favourites persisted to AsyncStorage with pre-hydration op replay.
- (decision) ADR-010 telemetry: counters in refs/plain integers, 1Hz flush, separate store; one-way dependency, market path never reads telemetry.
- (decision) ADR-011 MarketDataSource with Binance + simulator; MARKET_SOURCE env at startup; no automatic fallback; active source in market.status and /health, UI shows SIMULATED chip.
- (decision) [ADOPTED] ADR-012 pnpm 12.4.2 workspace, nodeLinker hoisted, zod pinned at root, contracts exports TS source, Metro watchFolders.
- (version) pnpm 12.4.2, Node 22.23.0, TypeScript 6.0.3, Expo SDK 57.0.24, RN 0.86.3, React 19.2.3, Fastify 5.12.5, @fastify/websocket 11.3.1 (ws 8.21.3), pino 10.3.1, Zod 4.6.5, tsx 4.23.13, ESLint 10.10.0 - from pnpm-lock.yaml.
- (constraint) Reanimated not used: importing it inflates Android memory on RN>=0.85 (Hermes); core Animated with native driver suffices (web search 2026-09-19).
- (assumption) Node 22 global WebSocket (undici) auto-answers Binance pings; verify in a >2 min soak run.
- (assumption) Pressure computed over the 10 displayed levels per side (spec assumption) while payload carries 20 levels (AGENTS row keys 0..19).
- (question) Stale threshold, backoff parameters, soft/hard buffer limits are config defaults, not measured; confirm in stress test.
- (event) Reviewer gate run: verification lens (12 findings, 2 high) + adversarial lens (30 findings, 3 critical, 10 high). Reviews in reviews/.
- (decision) Upstream client switched to ws 8.21.3 declared dep: Node global WebSocket never fires close on failed handshake, hides 451 (verified locally, undici #3546/#3697/#3506).
- (decision) Mobile coalesces inbound messages per pair, flushes once per requestAnimationFrame: RN socket has no flow control so server bufferedAmount cannot see slow JS thread.
- (decision) Applied adversarial tightenings A-01..A-30: rev scoped to connection; group-wise apply (book/ticker) never blanks; favourites absolute intents; heartbeatMs on wire; every source emits SourceStatusChange; interval null until set, ack via status; one fragment per pair lazily; staleness uses lastDepthAt; complete stalePairs; snapshot+status immediately on connect; downstream backoff reset after stable period, 4008 never resets; time-based eviction; paused state + generation ids; volume24h=q; DISPLAY_LEVELS constant; decodeServerMessage in contracts; maxPayload 4KiB.
- (question) OPEN (needs user approval): AGENTS.md says unknown narrowed by Zod parse, never cast; engineering-standards s6 envelope-only prod decode requires typed cast inside contracts decodeServerMessage. Add explicit exception to AGENTS.md?
- (question) OPEN (needs user approval): engineering-standards s3 says only MarketSnapshot leaves the adapter; architecture has adapter emit MarketEvent and gateway map PairState to PairSnapshot. Amend engineering-standards s3?
- (event) spine finalized: docs/architecture/architecture.md + adr/ADR-001..012; testing-strategy.md interval range updated to 10-1000ms.
- (decision) User approved: sole cast in contracts decodeServerMessage; exception added to AGENTS.md; engineering-standards s6 row references it; ADR-006 status updated.
- (decision) User approved: engineering-standards s3 amended - adapter emits MarketEvent, presentation maps PairState to PairSnapshot, domain never imports contracts.
